afficher les zones offres et entreprise que pour les utilisateurs valid sinon afficher une page de présentation de l'établissement
n'afficher le bouton de réponse d'offre que pour les étudiants valid
le bouton réponse d'offre ouvrira une page avec la possibilité de créer une réponse (cv, lettre de motivation):
    création pannel que pour les etudiants valid, mes réponses d'offres crud réponses d'offre

<div class="form-box m-1 p-3 preview">
    <h3 class="text-center">
        {{ offre.titre }}
    </h3>
    <p class="text-center">
        {{ offre.typeDeContrat }}<br>
    </p>
    <p class="text-center inside-preview-section">
        {{ offre.description }}<br>
    </p>
    <div class="row flex-nowrap content-edit-icon-card">
        <form class="col-4 icon" method="post" action="{{ path('app_registration_offer_delete', {'id': offre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ offre.id) }}">
            <button class="icon p-0 m-0 bg-transparent border-0" style="margin: 0 !important;">
                <img src="/assets/images/delete.png" alt="icon">
            </button>
        </form>
    </div>
</div>