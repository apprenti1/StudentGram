{% extends 'base.html.twig' %}

{% block title %}Edit Offre{% endblock %}

{% block body %}
    <h1>Edit Offre</h1>

    {{ include('registration_offer/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_registration_offer_index') }}">back to list</a>

    {{ include('registration_offer/_delete_form.html.twig') }}
{% endblock %}
