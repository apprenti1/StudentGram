
{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ dump(field_name) }}
    {# <input type="text" name="{{ field_name(offreForm.titre) }}"> #}
    <input type="submit">
    {{ form_end(form) }}



{% endblock %}


