{% extends 'base.html.twig' %}

{% block title %}TypeOffreEmploi index{% endblock %}

{% block body %}
    <h1>TypeOffreEmploi index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type_de_contrat</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_offre_emploi in type_offre_emplois %}
            <tr>
                <td>{{ type_offre_emploi.id }}</td>
                <td>{{ type_offre_emploi.typeDeContrat }}</td>
                <td>
                    <a href="{{ path('app_type_offre_emploi_show', {'id': type_offre_emploi.id}) }}">show</a>
                    <a href="{{ path('app_type_offre_emploi_edit', {'id': type_offre_emploi.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_type_offre_emploi_new') }}">Create new</a>
{% endblock %}
